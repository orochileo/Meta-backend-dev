Hypertext Markup Language:
Hypertext means text that contains links to other text. Markup refers to tags and elements used within a document. 
Most markups consist of: opening tag(<p>), closing tag(</p>). HTML elements can also contain other HTML elements, for example, 
"<p>This is a <i>Dynamic Programming</i> approach.</p>"
Self-closing element: For example, <br/> adds a line to the content
HTML specification is maintained by W3C. The current version is HTML 5.
